# Dotfiles for stateless Linux

These files are built from daily driver using chezmoi.  

The repo is intended to pull down to any machine or devcontainer.

These are not in github folder as sync'd via chezmoi.
